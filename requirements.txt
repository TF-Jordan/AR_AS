# ==============================================================================
# Requirements - AR_AS RaaS Platform
# ==============================================================================

# Core API
fastapi>=0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pydantic-settings==2.10.1
starlette==0.47.3

# Database PostgreSQL
sqlalchemy==2.0.43
asyncpg==0.31.0
psycopg2-binary==2.9.11
alembic==1.18.1

# Redis
redis==7.1.0

# Vector Database
qdrant-client==1.16.2

# ML & NLP (Modules 1 & 2)
# IMPORTANT: torch est installé en CPU-only (torch==2.9.1+cpu) dans le Dockerfile
# via --index-url https://download.pytorch.org/whl/cpu (~250 MB au lieu de ~2.5 GB)
# Pour installation locale CPU: pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==5.2.0
torch==2.9.1  # Version CPU installée via Dockerfile, voir ci-dessus
transformers==4.55.4
numpy==2.2.6

# Logging
python-json-logger==3.3.0
structlog==25.5.0

# Security & OAuth2
python-jose[cryptography]==3.5.0
PyJWT[crypto]==2.9.0
cryptography>=41.0.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
slowapi==0.1.9
bcrypt==5.0.0

# Utilities
python-dotenv==1.1.1
tenacity==9.1.2
httpx==0.28.1
